- if social_account_signed_in?
  -@offset = params[:offset]
  =render "messages/today"
  %br
- else
  %script
    Update();
  %br
  %span{ :style => "color:#f9bd27;"}
    %b Доброе
  время суток!
  %br
  Пожалуйста, зарегистрируйтесь через одну из социальных сетей:
  %span{ :style => "display:none" }
    = link_to "", {:controller => "main", :action => "home"}, :remote => true, :id => "update"
